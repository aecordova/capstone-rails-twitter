<!DOCTYPE html>
<html>
  <head>
    <%= render 'layouts/head' %>
    <body>
      <div class= "container-fluid d-flex">
        <%= content_tag :header, render('layouts/header'), class:'pr-0 col-2' if logged_in? %>
        <div class="main px-0 mx-auto w-100">
          <%= render 'layouts/sect_header' %>
          <%= yield %>    
        </div>
        <%= content_tag :div, '', class:'col-3' if logged_in? %>
      </div>
      <%= render 'layouts/js_for_bootstrap' %>
    </body>
  </html>
